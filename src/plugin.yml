name: GlowStick # the plugins name as it should appear in the plugin list /pl
version: 1.0 # the plugin's version
author: Deano3663
main: McEssence.GlowStick.Main
api-version: 1.18
commands:
  reload:
    description: reload Glowstick
    usage: /<command>
    permission: glowstick.admin.reload